package segundobi.exemplos.aulaseis;

public class ProcessaPedido {
    //A classe deve ter somente UMA responsabilidade
    //Responsabilidades que tem contexto de estar na classe
    //Assim como a classe deve ter somente UM motivo para mudar
    //Classes abertas = extensão // Classes fechadas = manutenção



    
//     public void process(Pedido pedido){

//             EnviarEmail enviarEmail = new EnviarEmail();
//             PgRepository pgRepo = new PgRepository();

//             boolean pedidoSalvoDB = pgRepo.salvar(pedido);

//             if (pedido.isValido() && salvar(pedido)) {

//             enviarEmail.enviarEmailPedidoConfirmado(pedido);
//         }


//         }

}

